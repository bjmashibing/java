#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../_pinyin@2.9.0@pinyin/bin/pinyin" "$@"
  ret=$?
else 
  node  "$basedir/../_pinyin@2.9.0@pinyin/bin/pinyin" "$@"
  ret=$?
fi
exit $ret
